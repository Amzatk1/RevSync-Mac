// Theme.swift
// Defines reusable colors and styles.
//

import SwiftUI

/// A central place for app‑wide styling values.
struct Theme {
    static let accentColor = Color.accentColor
    static let cardBackground = Color.gray.opacity(0.1)
}