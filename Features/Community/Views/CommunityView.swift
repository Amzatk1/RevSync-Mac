// CommunityView.swift
// Shows community forums or posts.
//

import SwiftUI

/// A placeholder view for the community section.
struct CommunityView: View {
    var body: some View {
        Text("Community")
            .font(.title)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
